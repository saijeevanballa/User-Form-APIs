{
  "version": 2,
  "name": "User-Forms-Apis",
  "builds": [
    {
      "src": "src/**/*.ts",
      "use": "@now/node-server"
    }
  ],
  "scripts": {
    "now-build": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/index.ts"
  },
  "build": {
    "env": {}
  }
}
